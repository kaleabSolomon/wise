# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo
# Viewer client bundle emitted next to its source assets during `pnpm dev`
src/viewer/public/app.js
src/viewer/public/app.js.map

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
logs/

# Test / coverage
coverage/
.vitest/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
Thumbs.db

# Local SQLite stores (should never live in a repo anyway)
*.sqlite
*.sqlite3
*.db

# Planning / design docs — kept local, never tracked.
# Shipped docs are exempted: they are read by people who install this,
# not just by whoever is sitting at this machine.
*.md
!README.md
!extension/CHANGELOG.md

# Astro generated types (site/)
.astro/

# Extension bundle (built by esbuild) and its package
extension/out/
*.vsix
