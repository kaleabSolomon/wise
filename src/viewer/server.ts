/**
 * Localhost viewer HTTP endpoints (Hono): list, detail, diffs. Reads straight
 * from the SQLite store. See TASKS.md Section 5.
 */

// TODO(Section 5): Hono app with list/detail/diff endpoints.
export {};
