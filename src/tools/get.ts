/**
 * `get_explanation` — retrieve by query / file / symbol; runs check-on-read
 * staleness; returns refresh materials if stale. The reading agent regenerates
 * and calls `save_explanation` back. See TASKS.md Section 3.
 */

// TODO(Section 3): implement get_explanation (check-on-read + refresh materials).
export {};
