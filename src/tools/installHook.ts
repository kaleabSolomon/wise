/**
 * `install_hook` — opt-in: drop the `post-commit` hook into one specified repo's
 * `.git/hooks/`. Never auto-runs; the only code path that touches a repo's
 * `.git/`. See TASKS.md Section 4.
 */

// TODO(Section 4): implement install_hook.
export {};
