/**
 * SQLite connection + schema. See TASKS.md Section 1.
 *
 * The store is a single SQLite file in `~/.wise`, living entirely
 * outside any repo. This module owns opening/creating it and applying the schema.
 */

// TODO(Section 1): open/create the SQLite file and apply the schema.
export {};
