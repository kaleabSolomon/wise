#!/bin/sh
# Wise — post-commit hook (INERT TEMPLATE).
#
# This file is never executed from the source tree. It is written into a target
# repo's .git/hooks/post-commit ONLY by an explicit `install_hook` call.
# See TASKS.md Section 4.
#
# TODO(Section 4): flag rows stale for symbols changed in the last commit.
exit 0
