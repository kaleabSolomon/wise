/**
 * Structural hash of an AST node — formatting- and comment-insensitive, so only
 * real changes register as staleness. See TASKS.md Section 2.
 */

// TODO(Section 2): compute a structural hash of a node.
export {};
